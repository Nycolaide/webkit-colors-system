export type ColorDataType = {
  [key: string]: {
    defaultLight: string;
    defaultDark: string;
    accessibleLight: string;
    accessibleDark: string;
  };
};

export type ColorListType = {
  [key: string]: string;
};
